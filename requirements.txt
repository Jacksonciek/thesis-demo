# ============================================================
#  Hate Speech Demo — requirements.txt
#  Versi library ini diselaraskan persis dengan pipeline
#  pelatihan NB02–NB05 untuk menghindari incompatibility.
# ============================================================
flask>=3.0.0

# --- Core deep learning ---
torch>=2.0.0
transformers==4.44.2
tokenizers>=0.19.0

# --- CRF layer (wajib: arsitektur BloomForMTL_v2) ---
pytorch-crf==0.7.2
# --- Interpretability ---
lime==0.2.0.1
scikit-learn>=1.3.0

# --- Data ---
numpy>=1.24.0
pandas>=2.0.0

# --- Visualization ---
matplotlib>=3.7.0
seaborn>=0.12.0
# BUG FIX: Pillow wajib ada — dipakai oleh lime_viz.fig_to_pil()
Pillow>=9.0.0

# --- UI (Gradio — runs fully locally, no hosting needed) ---
gradio>=4.0.0

# --- Download helper ---
gdown>=4.7.1

# --- Evaluation metrics (opsional, dipakai di seqeval report) ---
seqeval>=1.2.2
